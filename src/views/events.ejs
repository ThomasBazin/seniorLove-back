<%- include ('partials/navBar') %>

  <main class="container mt-4 d-flex">
    <div class="container-fluid">
      <div class="row">
        <%- include ('partials/sideNav') %>
          <!-- Main Content -->
          <div class="main_container col-md-9 p-3">
            <h2>Liste des événements</h2>
            <p>Nombre d’événements : <%=events.length %>
            </p>
            <table class="table table-striped text-center fw-medium">
              <thead>
                <tr>
                  <th>#id</th>
                  <th>Nom</th>
                  <th>Date</th>
                  <th></th>
                  <th></th>
                </tr>
              </thead>
              <tbody>
                <% events.forEach((event)=> { %>
                  <tr>
                    <td>
                      <%= event.id %>
                    </td>
                    <td>
                      <%= event.name %>
                    </td>
                    <td>
                      <%= new Date(event.date).toLocaleDateString('fr') %>
                    </td>
                    <td class="fw-bold text-gray-900">
                      <a href="#"
                        class="btn btn-link text-decoration-none d-flex justify-content-center align-items-center gap-2 ">
                        <img src="/logos/edit-2.svg" alt="edit">
                        <p class="mb-0">Modifier</p>
                      </a>
                    </td>
                    <td>
                      <a href="#"
                        class="btn btn-link text-decoration-none d-flex justify-content-center align-items-center gap-2">
                        <img src="/logos/trash-2.svg" alt="supprimer">
                        <p class="mb-0">Supprimer</p>
                      </a>
                    </td>

                  </tr>
                  <% }) %>
              </tbody>
            </table>
          </div>
      </div>
    </div>
  </main>

  <%- include('partials/footer') %>